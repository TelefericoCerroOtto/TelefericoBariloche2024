# Normative D01-D93 Traceability

Owners: **P** CMS; **H** HTTP/app/E2E; **M** core; **A** worker/infra; **R** renderer; **T** migration/rollout; **X** threat RED. Format: `decision — owner; validation`.

- **D01** — H active-code resolver; H/X route+QR E2E. **D02** — H QR-only access; H E2E. **D03** — M valid-QR population; M tests. **D04** — P no visitor/ticket; P catalog. **D05** — P ES/EN/PT version; P completeness. **D06** — P ordered aspects; P evolution. **D07** — P semantic key; P lifecycle. **D08** — P sole pointer/immutability; P transaction. **D09** — P point-only QR; P/H repoint.
- **D10** — H 1,800s grace; H boundaries. **D11** — H locales/draft; H UI/E2E. **D12** — P point fields; P schema. **D13** — P/H inactive intake/history; P/H lifecycle. **D14** — T replace/relocate; T migration. **D15** — H/P rating 1..5; H/P rejection. **D16** — H/P 1..3 total selections; H/P cardinality. **D17** — H/P separate `otherAspect`; H/P validation. **D18** — H/P separate comment; H/P contract. **D19** — P rating snapshot; P immutability.
- **D20** — P server context/acceptance; P auth/lifecycle. **D21** — H two-hour draft; H component. **D22** — H/A 24-hour guard; H Redis/clock. **D23** — H/P pseudonym/receipt/key; H/P replay. **D24** — H Redis fail-open order; H test. **D25** — P indefinite originals; P retention. **D26** — A model-copy redaction; A privacy. **D27** — P/H actor split; P auth. **D28** — H capabilities/no management; H/P matrix. **D29** — P Super Admin; P permissions.
- **D30** — P/H global/audit; P/H cross-user. **D31** — H/P five capabilities; H/P exhaustive. **D32** — H mediation/secrets; H/X boundary. **D33** — H public payloads/410; H routes. **D34** — H separate resources; H inventory. **D35** — A private OIDC worker; A/X OIDC. **D36** — P five collections; P catalog. **D37** — P settings singleton; P singleton. **D38** — P two components; P cardinality. **D39** — P excluded models; P catalog.
- **D40** — P run/report split; P lifecycle. **D41** — M Buenos Aires dates; M boundaries. **D42** — M preceding period; M properties. **D43** — M/P cutoff; M/P tests. **D44** — P active-range index; P race. **D45** — H/P regeneration/report; H/P overlap. **D46** — P/H retry lineage; P/H lineage. **D47** — H/P intersections/digest; H/P overlap. **D48** — P/A required-stage failure; P/A test. **D49** — M metric authority; M reconciliation.
- **D50** — M snapshot/comments; M schema. **D51** — M/A all comments; M/A coverage. **D52** — A token routing; A tests. **D53** — A budget formula; A tokens. **D54** — A source language/Spanish; A language. **D55** — A memory ref-map/sanitized checkpoint refs; A privacy. **D56** — A version/source; A config/checkpoints. **D57** — A gated Vertex model; A gate. **D58** — A settings; A config. **D59** — A output budgets; A limits.
- **D60** — A map/reduce duties; A schemas. **D61** — A digest graph/partial retry/reuse; A transitions. **D62** — A descriptive Spanish; A validator. **D63** — A no verbatim checkpoint/publication; A privacy. **D64** — M/A insufficient evidence; M/A thresholds. **D65** — M/A signal separation; M/A validator. **D66** — M/A four-ref minority; M/A boundary. **D67** — M/A recurrent formula; M/A boundary. **D68** — A seven sections; A schema. **D69** — P/H four states/queued failure; P/H lifecycle.
- **D70** — A create/delivery dedup+resume; A/X delivery. **D71** — P process/artifact relation; P atomicity. **D72** — A safe logs; A capture. **D73** — A alert after failed commit; A dedup. **D74** — H/A safe failure/run ID; H/A errors. **D75** — A usage/pricing; A cost. **D76** — A labels; A config. **D77** — A USD10 alert; A boundary. **D78** — A indefinite validated checkpoints/30-day diagnostics; A retention. **D79** — A bucket/prefixes; A IAM.
- **D80** — R pinned PDF; R repeats. **D81** — R eight sections; R structure. **D82** — R five charts; R inventory. **D83** — R evidence-first/no comments; R publication. **D84** — R conditional POC/no substitute; R gate. **D85** — M pure core; M graph. **D86** — A isolated worker/no metrics; A boundary. **D87** — M/A immutable handoff; M/A digest. **D88** — T app-local workspace; T manifest. **D89** — R/A worker image; R isolation.
- **D90** — T compatibility/rollout; T matrix. **D91** — T closed QR-only architecture; T lint. **D92** — P/H/M/A/R/T complete bundle; T manifest. **D93** — T block apply until tasks; T gate.

Audit: **93 covered, 0 missing, 0 duplicated, 0 contradicted**. Specs remain behavioral authority.
