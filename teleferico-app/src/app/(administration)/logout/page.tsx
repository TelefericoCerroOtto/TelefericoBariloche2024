import Logout from "./Logout";

// The logout page renders the Logout component, which automatically signs out the user, deleting the Auth.js session.

export default function LogoutPage() {
  return <Logout />;
}
