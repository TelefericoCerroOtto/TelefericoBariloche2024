# Base url of the frontend
NEXT_PUBLIC_BASE_URL=NEXT_PUBLIC_BASE_URL

# Base url where strapi is hosted
BUILD_STRAPI_BASE_URL=BUILD_STRAPI_BASE_URL

# Image bucket URL from GCP (used by Strapi), configured in Next.js to enable image loading.
BUILD_STRAPI_BUCKET_HOSTNAME=BUILD_STRAPI_BUCKET_HOSTNAME
BUILD_STRAPI_BUCKET_PATHNAME=BUILD_STRAPI_BUCKET_PATHNAME

# https://authjs.dev/getting-started/deployment#auth_trust_host
AUTH_TRUST_HOST=AUTH_TRUST_HOST
AUTH_SECRET=AUTH_SECRET

# reCaptcha credentials
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=NEXT_PUBLIC_RECAPTCHA_SITE_KEY # Used to generate the token
RECAPTCHA_SECRET_KEY=RECAPTCHA_SECRET_KEY # Used to validate the generated token
RECAPTCHA_API_KEY=RECAPTCHA_API_KEY # Used to autheticate the token validation request

# Variables for the contact form, obtained from the app registered in Google Auth Platform
CLIENT_ID=CLIENT_ID # Unique ID of your application in Google Cloud. Identifies your app to Google.
CLIENT_SECRET=CLIENT_SECRET # Secret key linked to the CLIENT_ID. Verifies that the request comes from your app.
REDIRECT_URI=REDIRECT_URI # URL where Google redirects the user after they approve or deny permissions. Must exactly match the one registered in Google Auth Platform.
OAUTH_ACCESS_TOKEN=OAUTH_ACCESS_TOKEN # Short-lived token that authorizes your app to access Google APIs on behalf of the user.
OAUTH_TOKEN_EXPIRY_DATE=OAUTH_TOKEN_EXPIRY_DATE # Expiry date of the access token in timestamp format 
OAUTH_REFRESH_TOKEN=OAUTH_REFRESH_TOKEN # Long-lived token used to generate new access_tokens without user interaction. Does not expire unless access is revoked or credentials are regenerated. Only issued once per user and app.
GMAIL_SENDER=GMAIL_SENDER # Authorized email address used to send messages from the form. Must match the authorized user address in GAP.
GMAIL_RECEIVER=GMAIL_RECEIVER # Email address that will receive the messages sent through the form.
