'use strict';

/**
 * component-translation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::component-translation.component-translation');
