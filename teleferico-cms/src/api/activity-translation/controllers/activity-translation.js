'use strict';

/**
 * activity-translation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::activity-translation.activity-translation');
