'use strict';

/**
 * activity-translation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::activity-translation.activity-translation');
